#ifndef VECTOR_COMMON_H
#define VECTOR_COMMON_H

#include <cassert>
#include <math.h>
#include <iomanip>

// Floating point precision for output
#define VECTOR_PRINT_PRECISION 4

// Prototypes
template<typename T> struct vec1base;
template<typename T> struct vec2base;
template<typename T> struct vec3base;
template<typename T> struct vec4base;

#endif // VECTOR_COMMON_H